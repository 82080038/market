refactor: update script fallback paths — PostgreSQL via settings.db_path

Scripts now resolve DB path via `settings.db_path` (from .env) instead of
hardcoded `data/market_research.db` (SQLite legacy file no longer exists).

Changes:
- 11 scripts: `os.environ.get("DB_PATH", "data/market_research.db")` →
  `os.environ.get("DB_PATH") or settings.db_path`
- 4 scripts: hardcoded `Path(...) / "data" / "market_research.db"` →
  `settings.db_path`
- 5 scripts: argparse `--db default="data/market_research.db"` →
  `default=None` + resolve via settings
- 4 shell scripts: removed `market_research.db` from sync/restore lists
- 23 scripts: docstring usage examples updated to DATABASE_URL
- `config.py`: added `script_db_path` property for legacy scripts
- `backfill_forex.py`: removed redundant fallback to market_research.db

One-shot migration scripts (migrate_sqlite_to_pg.py, migrate_foreign_flow.py,
backfill_news_sentiment.py, migrate_sqlite_to_pg_tables.py) intentionally
left as-is — they reference SQLite as the migration SOURCE.

86 scripts compile-checked. 131 focused tests pass.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
