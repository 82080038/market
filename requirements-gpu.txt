# GPU dependencies for PyTorch training/inference on cuda:1
# Install separately with: pip install -r requirements-gpu.txt
# or use uv pip: uv pip install -r requirements-gpu.txt --system
--index-url https://download.pytorch.org/whl/cu121
torch==2.3.1+cu121
torchvision==0.18.1+cu121
transformers>=4.42
